#!/usr/bin/sh

# useful when removed some file(s) from repo and don't want to remove the
# symlinks by hand

# removes all broken symlinks recursively

cd $HOME
find . -path ./.local/share -prune -o -path ./.cache -prune -o -xtype l -print |
  xargs rm
