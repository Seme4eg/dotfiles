# DO NOT EDIT
# generated by pywal cuz swaylock doen't support config files merging and i
# can't split 'theming' and 'main' configs

# ------ main config ------

daemonize
ignore-empty-password
show-keyboard-layout
indicator-caps-lock
font=TrainOne
font-size=50px

# Sets the indicator radius. The default value is 50.
# indicator-radius <radius>
# Sets the indicator thickness. The default value is 10.
# indicator-thickness <thickness>
# Use the inside color for the line between the inside and ring.
# line-uses-inside
# Use the ring color for the line between the inside and ring.
# line-uses-ring # <- this one's good

# swaylock-effects additional settings

clock
indicator
grace=5
effect-blur=8x2 # radius x times

# ------ theming ------

image={wallpaper}

# colors ea00ff 00e1ff (for testing)

# --- backspace highlight segments ---
bs-hl-color={color5.strip}99
caps-lock-bs-hl-color={color5.strip}99

# --- key press highlight segments ---
key-hl-color={color6.strip}99
caps-lock-key-hl-color={color6.strip}dd

# --- inside of the indicator ---
inside-color={background.strip}77
inside-clear-color={background.strip}55
inside-caps-lock-color={background.strip}99
# verifying
inside-ver-color={background.strip}77
inside-wrong-color={background.strip}99

# --- box containing the layout text ---
layout-bg-color=00000000
layout-border-color=00000000
layout-text-color={foreground.strip}

# --- line between the inside and ring ---
line-color=00000000
line-clear-color=00000000
line-caps-lock-color=00000000
# verification
line-ver-color=00000000
line-wrong-color=00000000

# --- ring of the indicator ---
# typing or idle
ring-color={background.strip}22
# cleared
ring-clear-color={background.strip}55
ring-caps-lock-color={background.strip}22
# verifying
ring-ver-color=00000000
ring-wrong-color={background.strip}99

# --- lines that separate highlight segments ---
separator-color=00000000

# --- text ---
text-color={foreground.strip}
text-clear-color={foreground.strip}
text-caps-lock-color={foreground.strip}
text-ver-color={foreground.strip}
text-wrong-color={foreground.strip}
